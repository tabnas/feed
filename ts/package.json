{
  "name": "@tabnas/feed",
  "version": "0.6.7",
  "description": "Parses RSS (0.90, 0.91, 0.92, 1.0, 2.0) and Atom (0.3, 1.0) feeds into a typed structure, defaulting to a normalized Atom shape.",
  "author": "Richard Rodger (http://richardrodger.com)",
  "license": "MIT",
  "main": "dist/feed.js",
  "types": "dist/feed.d.ts",
  "homepage": "https://github.com/tabnas/feed",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/feed.git",
    "directory": "ts"
  },
  "keywords": [
    "rss",
    "atom",
    "rdf",
    "feed",
    "parser",
    "tabnas",
    "xml"
  ],
  "scripts": {
    "pretest": "npm run build && node ../scripts/fetch-corpus.mjs all",
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "pretest-some": "npm run build && node ../scripts/fetch-corpus.mjs all",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "watch": "tsc --build src test -w",
    "build": "tsc --build src test",
    "clean": "rm -rf dist dist-test node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-stage-list": "npm stage list",
    "repo-stage-approve": "npm stage approve"
  },
  "peerDependencies": {
    "@tabnas/jsonic": ">=0",
    "@tabnas/parser": ">=0",
    "@tabnas/xml": ">=0"
  },
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/jsonic": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@tabnas/support": "*",
    "@tabnas/xml": "*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "files": [
    "src",
    "dist",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24"
  }
}
