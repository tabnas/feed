lib-cov
*.seed
*.log
*.csv
*.dat
*.out
*.pid
*.gz

pids
logs
results

npm-debug.log

*~
node_modules

.idea/


trial

test/coverage.html

coverage

dist
dist-test
*.tsbuildinfo

package-lock.json
yarn.lock

# Go
go/vendor/

# Third-party conformance corpora — FETCHED at a pinned commit, never
# committed. See scripts/fetch-feedparser.sh and scripts/fetch-feedvalidator.sh
# (both wrap scripts/fetch-corpus.mjs, which holds the pinned SHAs).
test/feedparser/
test/feedvalidator/

# Vale's downloaded style packages. The vocabulary under
# .vale/styles/config/ and the house rule under .vale/styles/Tabnas/
# are ours and ARE committed; Google/ arrives from the pinned URL
# in .vale.ini via `vale sync`.
/.vale/styles/Google/
